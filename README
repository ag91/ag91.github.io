Personal site of Andrea , managed by emacs, org mode, git and org-page.

This git repository is generated by org-page "op/new-repository" function, it is only used for demonstrating how the git branches and directory structure are organized by org-page.